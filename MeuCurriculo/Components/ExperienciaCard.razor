@using MeuCurriculo.Models

<div class="card mb-4 border-0 border-start border-4 border-primary shadow-sm">
    <div class="card-body">
        <div class="d-flex justify-content-between border-bottom pb-2 mb-2">
            <h5 class="fw-bold text-primary mb-0">@Dados.Cargo</h5>
            <span class="badge bg-secondary">@Dados.Inicio - @Dados.Fim</span>
        </div>
        <h6 class="fw-semibold">@Dados.Empresa</h6>
        <p class="text-muted small">@Dados.Descricao</p>
    </div>
</div>

@code {
    [Parameter] public Experiencia Dados { get; set; } = new();
}